# ReasonDraw

## Get Started:

```sh
git clone https://github.com/kenwheeler/ReasonDraw.git
cd ReasonDraw
npm install
npm start
```

Click and drag to draw. `esc` to clear your canvas. `q` and `w` to decrease/increase brush size, respectively.
